# State

_Pre-launch scaffolding. Target launch: this week (README.md "Status")._

## Current state

- Repository contains the launch infrastructure only: launch-readiness CI (`.github/workflows/launch-readiness.yml`, audit → harden → package), `scripts/launch/{audit,harden,package}.sh`, a scaffold `Dockerfile`, `docker-compose.yml` (app + Postgres 16.4), `LAUNCH_CHECKLIST.md` runbook, `.env.example`, `.github/dependabot.yml`.
- **No application code exists.** README.md:22-31 labels the stack "suggested, not yet implemented"; there is no `package.json`, no `src/`, no `app/`.

## Broken / incomplete

- `Dockerfile` is a placeholder: `npm run build || true` swallows build failures (Dockerfile:17) and `CMD ["node", "server.js"]` (Dockerfile:34) references a file that does not exist.
- The audit that gates main currently fails on 5 critical gaps: application source, dependency manifest, lockfile, test suite, health endpoint (scripts/launch/audit.sh:56-66, 108-110, 125-127). The workflow fails main while critical gaps remain (audit.sh:254-258).
- No test framework, linter, formatter or typecheck config — all audit warnings (audit.sh:112-122).
- Two open dependabot PRs (#5 node base image, #6 actions group) target an app that does not build yet.
- `launch-readiness-report.md` is generated by the audit, not committed; run `bash scripts/launch/audit.sh` for the current gap list.

## Blockers

- None blocking work. Human decision required first: freeze the v1 stack and surface (LAUNCH_CHECKLIST.md §1, "lock by Mon").

## Test command

None yet — no test framework exists; audit.sh requires a test suite (scripts/launch/audit.sh:108). After bootstrap: `npm test`.

## Run command

`docker compose up --build` (README.md:40) — or natively once package.json exists: `npm install && npm run dev` (README.md:43-44). App runs at http://localhost:3000.
